@model Capstone.Web.Models.Table

@{
    ViewBag.Title = "Table";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section>
    <h2>@Model.Name</h2>

    @foreach (var seat in Model.Seats)
    { 
    <p>@seat.Username</p>
    } 

</section>

